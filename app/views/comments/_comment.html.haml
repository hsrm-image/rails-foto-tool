.comment
  %p
    %b= comment.username 
    %span> &nbsp(
    = time_ago_in_words(comment.created_at)
    ago)

  %p
    = comment.text

  - if same_session?(comment.user_id) or admin?
    = button_to 'Delete', image_comment_url(@image, comment), method: :delete, data: { confirm: 'Are you sure?' }


