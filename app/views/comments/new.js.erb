<% if @comment.errors.any? %>
    <% @comment.errors.full_messages.each do |msg| %> 
        window.toastr.error("<%= escape_javascript (msg) %>", "", {"positionClass": "toast-bottom-center"});
    <% end %>
<% else %>
    //$("#comments").append(); TODO
    location.reload(); // HACK
    
<% end %>

