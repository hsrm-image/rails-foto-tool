<!DOCTYPE html>
<html>
  <head>
    <title>RailsFotoTool</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body 
  <% if content_for?(:body_attributes) %>
    <%= yield(:body_attributes) %> 
  <% end %>>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <p><%= link_to 'Log in', new_user_session_path%> <%= link_to 'Sign Out', destroy_user_session_path%> <%= link_to 'Edit account', edit_user_registration_path%></p>
    <p><%= link_to 'Users', users_path%> <%= link_to 'Images', images_path%> <%= link_to 'Collections', collections_path%></p>
    <%= yield %>
  </body>
</html>
